{
  "name": "facebook-handshake",
  "version": "2.1.3",
  "description": "Seamlessly log into Facebook silently, with a popup or with a redirect on unsupported browsers (Chrome/iOS).",
  "main": "dist/index.es5.js",
  "author": "Federico Brigante <npm@bfred.it> (https://twitter.com/bfred_it)",
  "license": "MIT",
  "jsnext:main": "index.js",
  "scripts": {
    "build": "rollup-babel-lib-bundler --lib-name 'index.es5' --format cjs --dest ./dist ./index.js",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "babel-preset-es2015-rollup": "^1.1.1",
    "rollup-babel-lib-bundler": "^2.3.6"
  },
  "babel": {
    "presets": [
      "es2015-rollup"
    ]
  },
  "dependencies": {
    "build-url-query": "^1.0.0",
    "facebook-sdk-promise": "^1.1.1"
  }
}
